# Copy to .env.local and fill in. The OpenAI key is server-side only:
# it must never be exposed to the client, so no NEXT_PUBLIC_ prefix.
OPENAI_API_KEY=

# Prisma. SQLite in dev; swap the connection string for Postgres at deploy.
DATABASE_URL="file:./dev.db"
